まずは tmp/36-session-summary-and-next-tasks.md を読んで、これまでやってきたことなどを把握してください。

---

どこかで、chorme を立ち上げるときに stdio の pipe を使うか websocket を使うか見たいな調査結果を tmp ディレクトリにまとめてくれたような気がするんですが、ありますか？

---

Chromium, Firefox, WebKit で、どういったプロトコルで Node.js（Playwright）とブラウザが通信しているか、実際の実装コード読みながら見ていきます。

まず Chromium ですが、起動オプションに --remote-debugging-pipe または --remote-debugging-port を使うことで、Pipe 通信または WebSocket 通信が選択できるとのことですが、この実装はどこにありますか？

---

packages/playwright-core/src/server/browserType.ts の以下コードにある`stdio[3], stdio[4]`とは何でしょうか？なぜ 3 と 4 なのでしょうか？

```ts
const stdio = launchedProcess.stdio as unknown as [
  NodeJS.ReadableStream,
  NodeJS.WritableStream,
  NodeJS.WritableStream,
  NodeJS.WritableStream,
  NodeJS.ReadableStream
];
transport = new PipeTransport(stdio[3], stdio[4]);
```

---

test-programmatic-api.js で Chromium を起動する際に、Pipe 通信か CDP かを指定する方法はありますか？

---

Chromium を操作する際に、Pipe か CDP かによって、コマンドの形式が変わることはありますか？
結局これら二つは通信の違いであって、どちらも Chrome Driver に同じコマンドを送ることになりますか？

---

crConnection.ts で、統一的にコマンドを送信するのに違和感を感じました。
Playwright はクロスブラウザをサポートしているので、統一的にコマンドを送信できないのではと思ったのですが

---

playwright/packages/playwright-core/src/server/transport.ts に定義された ProtocolRequest は全ブラウザ共通のように感じていて、かつ、CRConnection ではこの ProtocolRequest をそのまま send してそうですが、この ProtocolRequest は CDP ではなく全ブラウザ共通なのでしょうか？

---

実際に Chromium と Playwright で CDP ベースのコマンドでやりとりしてるかどうかを確認できる実装箇所はありますか。
