playwright はどのようにクロスブラウザ対応しているのか、これまで調べた内容が tmp/にまとまっています。

直近の内容が`tmp/30-next-session-investigation-tasks.md`に纏まってるので、まずはこれを読んでもらえますか

---

2.  Playwright の実行からブラウザ接続までの完全フロー - ユーザー API から実際の接続まで

からお願いします。引き続き、調査した内容や該当の実装箇所がわかるように tmp/にまとめてください

---

1. ブラウザ種類別の実装ディレクトリの使い分け - どこでブラウザごとの実装が選択されるか
2. Playwright の実行からブラウザ接続までの完全フロー - ユーザー API から実際の接続まで
3. PipeTransport の Stream 実装詳細 - Node.js の Stream を使った通信の仕組み
4. Safari WebKit 実装の調査 - WebInspector プロトコルの実装

---

tmp/31-browser-launch-flow-complete-analysis.md と一緒に実際のコードを読んでるのですが、以下の記載でタスクの作成と実行をした後に

```md
`runner.ts:72-96` - タスクの作成と実行
```

```md
### 1.2 テストフィクスチャでのブラウザ起動

`packages/playwright/src/index.ts:101-127` - browser フィクスチャ定義
```

につながる実装はどこを読めばわかるでしょうか。

---

```md
7. **FixtureRunner が browser フィクスチャを初期化**
   - テスト関数の引数を解析
   - 必要なフィクスチャ（`browser`、`page`等）を依存関係順に解決
   - `browser`フィクスチャが要求された場合、上記 1.2 のフィクスチャ定義が実行される
```

とありますか、browser フィクスチャが要求された時に

以下の処理に行く経緯は実装のどこを読めばわかるでしょうか

`packages/playwright/src/index.ts:101-127` - browser フィクスチャ定義

```typescript
browser: [
  async (
    { playwright, browserName, _browserOptions, connectOptions },
    use,
    testInfo
  ) => {
    // browserNameに基づいてブラウザを選択
    if (!["chromium", "firefox", "webkit"].includes(browserName))
      throw new Error(`Unexpected browserName "${browserName}"`);

    // playwright[browserName].launch()を実行
    const browser = await playwright[browserName].launch();
    await use(browser);
    await browser.close({ reason: "Test ended." });
  },
  { scope: "worker", timeout: 0 },
];
```

---

## 2. プログラマティック API のエントリーポイント

をみる感じ、手元で気軽に試せそうなんですかね？

---

## 4. launch()メソッドの実装フロー

でクライアント側とサーバー側という表現がありますが、それぞれ具体的に何を指しているでしょうか？

クライアントがブラウザでサーバーが Node.js のプロセスという理解であってますか？

---

```
2. **プロセス起動**
   - `_launchProcess()`内で`launchProcess()`を呼び出し（browserType.ts:213-242）
```

の`launchProcess`では何をやっていますか？stdio pipe の起動でしょうか？
ブラウザとのプロトコルは stdio pipe や cdp などプロトコルがいくつかあると思うのですが、ここではどのように扱っているでしょうか？

---

lanuchProcess の中で通信プロトコルも選択されてるのでしょうか？

---

BiDi プロトコルも選択できるような実装があったように思うのですが
もしあるなら、実装箇所を教えてください

---

`_launchProcess`の中で BiDi プロトコルも条件分岐で選択できるようになっているんでしょうか？

---

実際にコードを見つつ、playwright を実行してからどのようにブラウザを起動し、操作してるかがざっくりわかった

次回は、すでに調べてもらった内容もあるが、webkit と Firefox をどのように操作しているか、WebInspector プロトコルと Juggler プロトコルの仕組みを実際のコードを読みつつ、理解していきたい。

これを次回のセッションに引き継げるように tmp/にまとめてください
